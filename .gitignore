node_modules/
dist/
coverage/
playwright-report/
test-results/
.DS_Store
*.log

# Third-party assets are fetched, not committed: `npm run assets:fetch`.
# Keeping them out of git keeps the repository small and the licensing
# auditable from the manifest rather than from binary history.
public/assets/

# Local screenshot harnesses used while iterating on the look.
*.tmp.mjs

# `npm run shoot` output: screenshots, the contact sheet and the model
# catalogue. Regenerated on demand and far too large to keep in git.
shots/
